import { IonPickerValueAccessorDirective } from './ion-picker-value-accessor-directive';

describe('IonPickerValueAccessorDirective', () => {
  it('should create an instance', () => {
    expect(new IonPickerValueAccessorDirective()).toBeTruthy();
  });
});
